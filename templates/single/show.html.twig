{% extends 'base.html.twig' %}

{% block title %}Single{% endblock %}

{% block body %}
    <h1>Single</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ single.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ single.name }}</td>
            </tr>
            <tr>
                <th>Time</th>
                <td>{{ single.time }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('single_index') }}">back to list</a>

    <a href="{{ path('single_edit', {'id': single.id}) }}">edit</a>

    {{ include('single/_delete_form.html.twig') }}
{% endblock %}